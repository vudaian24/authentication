name: CD
on:
  workflow_call:

jobs:
  build-and-push:
    name: Build and Push
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

  deploy:
    needs: build-and-push
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
